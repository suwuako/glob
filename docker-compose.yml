services:
  glob:
    build: .
    image: glob
    volumes:
      - ./mnemosyne:/mnemosyne:z
    ports:
      - "5217:5217"
    networks:
      - shared-proxy

networks:
  shared-proxy:
    external: true
